name: Jekyll site CI

on:
  workflow_dispatch:
env: 
    SSHPASS: "zorrozorro123"
jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the site in the jekyll/builder container
      run:  |   
                sudo apt update
                sudo apt install sshpass
                sshpass -e scp -r ./* ubuntu@ec2-3-18-106-74.us-east-2.compute.amazonaws.com:/usr/share/nginx/html/
           
